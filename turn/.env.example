# Shared with signaling server (TURN_SECRET must match)
TURN_SECRET=deskly-dev-secret-change-me

# Your public IP (or LAN IP for local tests). Required for peers behind NAT.
# Example: EXTERNAL_IP=203.0.113.10
# For same-machine local test you can use 127.0.0.1
EXTERNAL_IP=127.0.0.1
